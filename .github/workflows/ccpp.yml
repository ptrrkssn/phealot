name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
    - uses: actions/checkout@v2
    - name: install dependencies
      run: test -f /usr/share/perl5/Net/DNS.pm || sudo apt-get install libnet-dns-perl
#    - name: configure
#      run: ./configure
#    - name: make
#      run: make
    - name: make check
      run: make check
#    - name: make distcheck
#      run: make distcheck
